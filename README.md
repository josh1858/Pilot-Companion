# FlightIntel X4
Working data build. Airport selection populates destination, weather fallback/live Open-Meteo, runway, wind, airport schematic, FTL latest off-blocks and links.

Open `index.html` directly or host via GitHub Pages. For PWA install, use HTTPS.
